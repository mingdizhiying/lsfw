export async function compressImage(file){if(!/^image\/(jpeg|png|webp)$/.test(file.type))throw Error('请选择 JPG、PNG 或 WebP 图片。');if(file.size>30*1024*1024)throw Error('原图请控制在30MB以内。');const image=await createImageBitmap(file);const scale=Math.min(1,2000/Math.max(image.width,image.height)),canvas=document.createElement('canvas');canvas.width=Math.round(image.width*scale);canvas.height=Math.round(image.height*scale);canvas.getContext('2d').drawImage(image,0,0,canvas.width,canvas.height);image.close();const blob=await new Promise(resolve=>canvas.toBlob(resolve,'image/webp',.85));if(!blob||blob.size>8*1024*1024)throw Error('图片压缩后仍过大。');return blob}
